{
  "name": "lixso",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && browserify src/main.ts -p [ tsify --project tsconfig.json ] > dist/main.js",
    "build-editor": "mkdir -p dist && browserify src/editor.ts -p [ tsify --project tsconfig.json ] > dist/editor.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "browserify": "^17.0.1",
    "esbuild": "^0.25.11",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "tsify": "^5.0.4",
    "typescript": "^5.9.3"
  }
}
